local E = require(script.E)

return {
	Keys = {
		E = {
			Hold = true,
			FaceMouse = true,
			FullOrientation = true,
			FaceSpeed = 2,
			
			Initiate = E.Initiate,
			Terminate = E.Terminate,
		}
	},
}

